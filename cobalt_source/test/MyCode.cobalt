
package test
import other
import compiler.structure.blocks.ifs.IfBlock
import test.MyCode

open class MyCode(xx:int, yy:int):
    /* Other test */
    //xx += 1

    // Test
    public:
        /*
        Another test
        */
        open def method1(stringTest:String) <- void:
            print(stringTest)
            x: int = 5
            //while (x < 10):
                //x += 1
                //print("Test")

        /**
        *Another test
        */
        def main() <- void:
            stringyThing: String = "Thisisastring"
            z: int = (10 + 25) * 5 - 3 + (7 * 5) + 5 + 5
            longTest: long
            ty: int = 15
            //myCode: MyCode = new MyCode()

            //w: float = 2.0f
            //myCode.method1(stringyThing)
            //myCode

    private:
        def privateMethod(y: int) <- void:
            yy: int = 10

    def noModifierMethod(z: int) <- void:
        test: int = 35
