package test.ifs

class IfElseTest():

    let main() <- void
        x:int = 50
        y:int = 75

        if (x < 100)
            if (y < 65)
                println("Hello World")

        if (y < x)
            println("Y is smaller than twenty")

        if (200 < y)
            println ("Y is smaller than two hundred")
        elif (x > 15)

        else
