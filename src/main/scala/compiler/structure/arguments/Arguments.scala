package compiler.structure.arguments

import scala.collection.mutable.ListBuffer

class Arguments {

  def getArguments(line: String): Unit = {

    // val arguments: ListBuffer[Argument] = line.split(",").toList

  }

}
