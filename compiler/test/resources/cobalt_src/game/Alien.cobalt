package game

class Alien(x: int, y: int) extends Sprite

x = 10

exampleMethod (a: Int, b: Int): Int

initAlien (x: int, y: int): void = do
    this.x = x
    this.y = y

act (direction: int): void
  x = x + direction


class ExampleClass

    let x = 10

    let mutable mutVar = 100

    let add(a: Int, b: Int) = do
        mutVar <- 1000
        a + b

