package integration

class Example(x: int, y: int) extends Sprite

    let inlineMethod(a: Int, b: Int): Int = 1

    let blockMethod(direction: int): void = do
        let x = 1
        2
        3
        32767

    let main(): void = do
        println (1)

