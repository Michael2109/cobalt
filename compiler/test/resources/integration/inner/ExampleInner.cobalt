package integration.inner

class ExampleInner(x: int, y: int) extends Sprite

    let x = 10

    let exampleMethod (a: Int, b: Int): Int = 1

    let initAlien (x: int, y: int): void = do
        let x = 10

    let act (direction: int): void = do
        let x = 10

    class ExampleClass

        let x = 10

        let mutable mutVar = 100

        let add(a: Int, b: Int) = do
            let mutable x = 10
